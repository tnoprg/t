CREATE TABLE IF NOT EXISTS t_user (user_id integer GENERATED BY DEFAULT AS IDENTITY, created_at TIMESTAMP NOT NULL,  stu_id varchar(255) NOT NULL, name varchar(255) NOT NULL, score integer, updated_at TIMESTAMP NOT NULL, VERSION integer NOT NULL, PRIMARY KEY (user_id))
